<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Bord à bord (targetSdk 36) : visible derrière la barre de statut et la barre
             de navigation une fois les marges appliquées par Capacitor. -->
        <item name="android:windowBackground">@color/edgeToEdgeBackground</item>
    </style>


    <!-- android:background dans un thème s'applique à TOUTES les vues, y compris
         les boîtes de dialogue système (le logo apparaissait dans le sélecteur
         de date). windowBackground ne touche que la fenêtre de l'activité. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>