# Gestionnaire de paquets du projet : pnpm (Vercel installe avec pnpm --frozen-lockfile).
# Ne pas réintroduire package-lock.json : deux verrous désynchronisés cassent le déploiement.
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  vue-demi: true
